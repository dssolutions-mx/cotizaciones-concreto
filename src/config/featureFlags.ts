export const features = {
  // Expose grouping UI under a dedicated page; permanently enabled in production
  masterGroupingEnabled: true,
  // Controls master-first pricing resolution in validators/order flows; permanently enabled in production
  masterPricingEnabled: true,
};


